spring:
  profiles:
    active: dev
camel.springboot.shutdownTimeout: 2
camel.springboot.main-run-controller: true

---

spring:
  profiles: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    jdbc-url:  jdbc:postgresql://localhost:5432/Camel
    username: postgres
    password: pathaks01
